# PAUL Handoff

**Date:** 2026-06-02
**Status:** paused — end of session, clean loop position

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Grano — granular synthesis audio plugin (VST3 + AU + Standalone)
**Stack:** C++20, JUCE 8, CMake, Catch2, GitHub Actions CI
**Core value:** Advanced granular sound design: dense particle clouds, sub-grain recursion, stochastic timing, internal feedback, spectral processing, audio-rate modulation.

---

## Current State

**Version:** v1.0.0-beta.1 (tagged)
**Phase:** F8 — QA and Manual Acceptance Testing — In Progress
**Plans done this session:** 08-02 ✅ 08-03 ✅ 08-04 ✅
**Plans pending:** 08-01 ○ (Windows testing checkpoint, human tester required)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [All loops closed — clean state]
```

---

## What Was Done This Session

- **08-02** — Built `ModuleTabPanel`: 6-tab panel (ENGINE|MOTION|COLOR|PATTERN|LFO|MOD) exposing all F4/F6 APVTS parameters that had zero UI. Active tab highlights via `setToggleState` + GranoLAF.
- **08-03** — Fixed all UI layout bugs found by human tester: ENGINE tab 3-column grid (selectors|feedback|spectral), MOTION row gap, masterVolKnob uniform width, PATTERN combo heights 8px→22px.
- **08-04** — Added 3 Catch2 audio smoke tests (RMS > 0, finite, peak < 2.0) using `GranularEngine::testSample_`. 121/121 tests pass.
- **VST3 category fix** — Changed `VST3_CATEGORIES "Instrument/Sampler"` → `"Fx"` and `AU_MAIN_TYPE` → Effect so Audacity and other hosts can insert on audio tracks.
- **Windows artifact** — CI built and tested Windows VST3. User loaded in Audacity, heard granular audio after loading sample into Grano's waveform display.

---

## What's Pending

### 08-01 — Windows testing checkpoint
The main remaining F8 task. Requires human tester on Windows to run all 88 cases in `TESTING.md`. Artifact is at `D:\temp\grano-windows\Grano.vst3` (or download fresh from CI: `gh run download <latest-run-id> -n Grano-VST3-Windows`).

**Key workflow for Audacity testing:**
- Grano does NOT process host audio — it generates from its own loaded sample
- Drag WAV into Grano's waveform display (or click S0-S3 in header) to load
- Click Preview in Audacity to trigger processBlock → grains appear → sound

**After Windows pass:** Plan 08-05 for macOS VST3 + AU + sign-off.

---

## What's Next

**Immediate:** Resume Windows testing — run TESTING.md 88 cases against `Grano.vst3`

**After that:** Plan 08-05 macOS testing → F8 complete → v1.0 milestone done

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/08-qa/08-01-PLAN.md` | Windows testing checkpoint plan |
| `TESTING.md` | 88 manual test cases |
| `D:\temp\grano-windows\Grano.vst3` | Latest Windows artifact |

---

## Architecture Reminders (F8-specific)

- **Grano is a generator, not an effect** — it ignores host audio input, generates from loaded samples
- `IS_SYNTH FALSE` — no MIDI note input; runs continuously once sample loaded
- `VST3_CATEGORIES "Fx"` — set this session so hosts treat it as audio effect (insertable on tracks)
- GranularEngine uses `testSample_` (built-in 440Hz sine) for unit tests — no file I/O needed

## Accumulated decisions to remember

- `spectralModeBox_` height: always use `comboH` not `comboH - labelH` (= 8px bug)
- ENGINE tab: 3-column layout — selectors (col1) | feedback (col2) | spectral (col3)
- Tab active state: `setToggleState(i == idx)` → GranoLAF handles mint fill + glow
- Audio smoke warmup: `Thread::sleep(200ms)` before processBlock loop (scheduler fill time)

---

## Resume Instructions

```
git pull
claude
/paul:progress
```

PAUL reads STATE.md and tells you exactly where to pick up.
Next action: run Windows TESTING.md suite (08-01 checkpoint) or `/paul:plan` for 08-05 macOS.

---

*Handoff created: 2026-06-02*
