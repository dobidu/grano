---
phase: 08-qa
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Tests/test_audio_smoke.cpp
  - Tests/CMakeLists.txt
autonomous: true
---

<objective>
## Goal
Add audio output smoke tests to Catch2: non-zero RMS, finite samples, no runaway clipping.
</objective>

<context>
@.paul/PROJECT.md
@Source/Engine/GranularEngine.h
@Tests/test_engine.cpp
</context>

<acceptance_criteria>

## AC-1: Engine produces non-zero audio
```gherkin
Given GranularEngine is prepared and running
When processBlock runs for 20 blocks after 200ms warmup
Then max RMS across all blocks > 1e-6
```

## AC-2: No NaN or Inf in output
```gherkin
Given GranularEngine running
When processBlock output is inspected sample-by-sample
Then all samples are finite (std::isfinite == true)
```

## AC-3: No runaway accumulation
```gherkin
Given GranularEngine running
When peak absolute value is tracked across 20 blocks
Then peak < 2.0 (overlapping grains cannot explode)
```

</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Write test_audio_smoke.cpp and register in CMakeLists</name>
  <files>Tests/test_audio_smoke.cpp, Tests/CMakeLists.txt</files>
  <action>Create 4 Catch2 tests using GranularEngine with built-in testSample_. No file I/O.</action>
  <verify>build/Tests/GranoTests "[audio]" — all pass</verify>
  <done>AC-1, AC-2, AC-3 satisfied; 121/121 total tests pass</done>
</task>
</tasks>

<output>
After completion, create `.paul/phases/08-qa/08-04-SUMMARY.md`
</output>
